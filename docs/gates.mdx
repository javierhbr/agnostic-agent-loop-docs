---
id: gates
title: The 5 Gates
description: Non-negotiable checkpoints enforced at CLI, CI, PR, and agent levels.
---

import { SectionHeader, GateCard, CalloutBox } from '@site/src/components/tom';
import styles from '@site/src/components/tom/tom.module.css';

<SectionHeader
  num="05"
  title="The 5 Gates"
  desc="If any gate fails, progress is blocked. No exceptions."
/>

<div className={`${styles.gatesGrid} fi`}>

<GateCard
  num="G1"
  title="Context Completeness"
  checks={[
    'All MCP sources cited with versions',
    'Context Pack version pinned in spec',
    'constitution.md exists and is non-empty',
  ]}
/>

<GateCard
  num="G2"
  title="Domain Validity"
  checks={[
    'No invariant violations',
    'Domain ownership respected',
    'No direct cross-domain database access',
  ]}
/>

<GateCard
  num="G3"
  title="Integration Safety"
  checks={[
    'All contract consumers identified',
    'Compatibility plan for breaking changes',
    'Dual-publish strategy defined',
  ]}
/>

<GateCard
  num="G4"
  title="NFR Compliance"
  checks={[
    'Logging, metrics, and tracing declared',
    'PII handling specified',
    'Performance targets set (p95)',
  ]}
/>

<GateCard
  num="G5"
  title="Ready to Implement"
  checks={[
    'No open blocked_by ADRs',
    'Spec is unambiguous',
    'All acceptance criteria are testable',
  ]}
/>

</div>

<CalloutBox type="red" label="Failure rule">
  If any gate fails, the system blocks progress until the failure is resolved. Enforcement happens through agent checklists and CI spec-link validation in both repos.
</CalloutBox>
