---
id: agents
title: The Four Agents
description: Analyst, Architect, Developer, Verifier — each a Markdown file with a bounded role and explicit exit gate.
---

import { SectionHeader, ToolCard, CalloutBox } from '@site/src/components/tom';
import styles from '@site/src/components/tom/tom.module.css';

<SectionHeader
  num="01b"
  title="The Four Agents"
  desc="Four Markdown agents define the process. Each role is bounded, each handoff has an exit gate, and each step is grounded in MCP context."
/>

<div className={`${styles.toolsGrid} fi`}>

<ToolCard
  accent="rust"
  badge="Analyst"
  title="analyst.md"
  layer="Full workflow only · high/critical"
  desc="Discovery role. Defines problem, evidence, affected components, and success metrics before architecture begins."
  items={[
    'Loads context with Platform MCP get_context_pack(intent)',
    'Fetches template with get_template("discovery")',
    'Produces discovery.md for Architect handoff',
    'Must complete analyst exit gate before handoff',
  ]}
/>

<ToolCard
  accent="navy"
  badge="Architect"
  title="architect.md"
  layer="Standard + Full workflows"
  desc="Specification role. Produces feature-spec and component specs, with explicit contracts, NFRs, and risk flags."
  items={[
    'Calls get_context_pack(intent) from Platform MCP',
    'Calls get_contracts(), get_invariants(), get_decisions() per component',
    'Produces one feature-spec.md plus component-spec.md per domain',
    'Declares contract changes and critical-risk flags explicitly',
  ]}
/>

<ToolCard
  accent="teal"
  badge="Developer"
  title="developer.md"
  layer="All workflows · one run per component"
  desc="Implementation role. Produces implementation plan and executes code in a single component scope."
  items={[
    'Calls get_patterns() for approved implementation patterns',
    'Calls get_decisions() to avoid contradicting ADRs',
    'Produces impl-spec.md + tasks.yaml in docs/specs/',
    'Runs in parallel across components after Architect fan-out',
  ]}
/>

</div>

<div className="fi" style={{marginTop: '1.5rem'}}>

<ToolCard
  accent="green"
  badge="Verifier"
  title="verifier.md"
  layer="All workflows · hard stop before merge"
  desc="Verification role. Confirms acceptance criteria with evidence and enforces sign-off policy by risk level."
  items={[
    'Loads DoD from Platform MCP get_context_pack(intent)',
    'Produces verify.md with pass/fail evidence per AC',
    'Blocks merge until all checks are complete',
    'Requires explicit HUMAN APPROVED record for critical changes',
  ]}
/>

</div>

<CalloutBox type="amber" label="Handoff contract">
  An agent never starts until the previous agent completes its exit gate. Quality is enforced at transitions, not after integration.
</CalloutBox>
