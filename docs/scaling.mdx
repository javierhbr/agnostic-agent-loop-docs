---
id: scaling
title: Scaling Guide
description: Start with one repo and add structure only when the team needs it.
---

import { SectionHeader, ScaleCard, D1Phase, CalloutBox } from '@site/src/components/tom';
import styles from '@site/src/components/tom/tom.module.css';

<SectionHeader num="08" title="Scaling Guide" desc="Start with one repo and add structure only when the team needs it. The two-MCP model and four agents never change — only the physical organisation of content does." />

<div className={`${styles.scaleGrid} fi`}>

<ScaleCard
  stage="early"
  title="Early Stage"
  teams="1–2 people"
  desc="One repo. Platform MCP reads a single openclaw-specs/ directory. Component MCP reads the one monorepo component you have. Quick workflow only."
  items={[
    'Single repo: specs + code together',
    'Platform MCP reads local directory',
    'One component MCP for the whole system',
    'Quick workflow only (developer → verifier)',
  ]}
/>

<ScaleCard
  stage="growing"
  title="Growing Team"
  teams="3–6 people"
  desc="Split repos. openclaw-specs/ becomes its own repo so product and engineering can PR separately. One component MCP instance per bounded context."
  items={[
    'openclaw-specs/ repo split from system repo',
    'Component MCP instances per major component',
    'Standard workflow introduced for new features',
    'agents/ symlinked into .claude/ from specs repo',
  ]}
/>

<ScaleCard
  stage="full"
  title="Full Model"
  teams="6+ people"
  desc="Full two-repo model as described in this document. Full workflow for high/critical. Human approval gates enforced at Verifier. Spec graph fully linked."
  items={[
    'Full workflow for high/critical risk',
    'Analyst agent activated for major initiatives',
    'Human approval gates enforced in verifier.md',
    'graph/index.yaml fully linked for audit trail',
  ]}
/>

</div>

<div className="fi" style={{ marginTop: '3rem' }}>

## Ready to start on Day 1?

Four phases. MCP servers first — then your first initiative — then parallel fan-out — then harden.

<div className={styles.d1Grid}>

<D1Phase
  label="Phase 1 · Foundation"
  items={[
    'Create openclaw-specs/ repo',
    'Write constitution/policies.md',
    'Deploy Platform MCP (reads specs/)',
    'Deploy Component MCP per component',
    'Symlink .claude/agents/ to specs/agents/',
  ]}
/>

<D1Phase
  label="Phase 2 · First Initiative"
  items={[
    'Select one low-risk pilot feature',
    'Run quick workflow: developer.md only',
    'Developer calls Component MCP',
    'Verifier produces verify.md',
    'Validate gate checklist end-to-end',
  ]}
/>

<D1Phase
  label="Phase 3 · Standard Flow"
  items={[
    'Run standard workflow for a medium feature',
    'Architect produces feature-spec.md',
    'Fan out: developer.md per component (parallel)',
    'Verifier waits for all developers done',
    'Update graph/index.yaml post-merge',
  ]}
/>

<D1Phase
  label="Phase 4 · Harden"
  items={[
    'Add full workflow for critical changes',
    'Analyst activated for Guaripolo/live flag',
    'Human approval gate in verifier.md',
    'CI enforces verify.md sign-off before merge',
  ]}
/>

</div>

</div>

<div className="fi" style={{ marginTop: '3rem' }}>

<CalloutBox type="amber">
  The two-MCP model and four agents **never change** as you scale. What changes is the physical organization of content across repos and the strictness of gate enforcement. The model adapts to the team — the team doesn't adapt to the model.
</CalloutBox>

</div>
