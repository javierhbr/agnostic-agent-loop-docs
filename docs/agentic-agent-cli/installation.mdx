---
id: installation
title: Installation
slug: /agentic-agent-cli/installation
description: Install Agentic Agent with Go, build from source, or run in dev mode.
---

import { SectionHeader, CalloutBox } from '@site/src/components/tom';

<SectionHeader
  num="14f"
  title="Installation"
  desc="Install the agentic-agent CLI and verify it runs on your machine."
/>

<CalloutBox type="teal" label="Diataxis">
  How-to. Use this when you need the binary installed or you are troubleshooting PATH issues.
</CalloutBox>

## Prerequisites

- Go 1.22+
- Git

## Option 1: Go install (recommended)

```bash
go install github.com/javierbenavides/agentic-agent/cmd/agentic-agent@latest
agentic-agent version
```

## Option 2: Build from source

```bash
git clone https://github.com/javierbenavides/agentic-agent.git
cd agentic-agent
make setup
agentic-agent version
```

## Option 3: Development (no install)

```bash
go run ./cmd/agentic-agent version
```

## Troubleshooting

If you see `agentic-agent: command not found`, ensure your Go bin directory is on PATH (or run via `go run`).

